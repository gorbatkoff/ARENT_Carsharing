import React, { memo, ReactNode } from "react";
import styles from "./Button.module.scss";
import classNames from "classnames";

function Test() {
  return <div>
    Test component v2.3 dev Az
    </div>; 
}

export default memo(Test);
